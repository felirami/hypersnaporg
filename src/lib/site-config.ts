/** Footer/nav links only — keep this file free of large source snapshots. */
export const siteConfig = {
  organizationUrl: "https://github.com/farcasterorg",
  publicNodeBaseUrl: "https://haatz.quilibrium.com",
} as const;
